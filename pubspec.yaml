name: idesk_live_chat
description: "Flutter package of IDesk live chat."

version: 0.0.3
homepage: https://github.com/Codeware-Ltd/idesk_live_chat_flutter.git

environment:
  sdk: ">=3.0.0-0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  flutter_inappwebview: 5.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
